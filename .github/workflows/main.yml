name: Main CI

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  web:
    uses: ./.github/workflows/web.yml

  android:
    uses: ./.github/workflows/android.yml
